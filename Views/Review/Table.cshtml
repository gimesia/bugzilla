@using System.Collections
@model IEnumerable<bugzilla.Models.Review>

@{
    ViewData["Title"] = "Reviews";
}
<html>

<head>
    <title>Reviews</title>
</head>
<body>
<div>
    <form asp-controller="Review" asp-action="Table" asp-route-fixerName="">
        <label for="reviewer">Review made by:</label>
        <select id="reviewer" name="reviewer" class="mr-4">
            <option value="*">all</option>
            @foreach (var dev in ((IEnumerable<Review>) ViewData["reviews"]).Select(review => review.Dev).Distinct())
            {
                <option value="@dev?.Name">
                    @dev?.Name
                </option>
            }
        </select>
        <label for="fixer">Fix made by:</label>
        <select id="fixer" name="fixer" class="mr-4">
            <option value="*">all</option>
            @foreach (var dev in ((IEnumerable<Review>) ViewData["reviews"]).Select(review => review.Fix?.Dev).Distinct())
            {
                <option value="@dev?.Name">
                    @dev?.Name
                </option>
            }
        </select>
        <button type="submit">
            Filter!
        </button>
    </form>
</div>
<table class="table table-hover">
    <thead>
    <tr>
        <th>
            @*@Html.DisplayNameFor(model => model.Id)*@id
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.Fix.Id)*@Fix made by
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.Dev.Name)*@Reviewed By
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Approved)
        </th>
        <th>
            <a asp-action="AddOrEdit" class="btn btn-outline-success"> New Review</a>
        </th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fix.Dev.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Dev.Name)
            </td>
           <td>
                @Html.DisplayFor(modelItem => item.Approved)
            </td>
            <td>
                <a asp-controller="Review" asp-action="AddOrEdit" asp-route-guid="@item.Id">
                    Edit
                </a>
                <a asp-controller="Review" asp-action="Delete" asp-route-guid="@item.Id" class="text-danger ml-1" onclick="return confirm('Are you sure to delete this record?')" >
                    Delete
                </a>
            </td>
        </tr>
    }
    </tbody>
</table>
</body>
</html>