@model IEnumerable<bugzilla.Models.Developer>

@{
    ViewData["Title"] = "Index";
}
<html>

<head>
    <title>Developers</title>
</head>
<body>
<table class="table table-hover">
    <thead>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Role.Name)
        </th>
        @*<th>
            <a asp-action="AddOrEdit" class="btn btn-outline-success"><i class="far fa-plus-square"></i> Employee</a>
        </th>*@
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Role.Name)
            </td>
            @*
            <td>
                <a asp-action="AddOrEdit" asp-route-id="@item.DevId">
                    <i class="fa fa-marker fa-lg"></i>
                </a>
                <a asp-action="Delete" asp-route-id="@item.DevId" class="text-danger ml-1" onclick="return confirm('Are you sure to delete this record?')">
                    <i class="fa fa-trash-alt fa-lg"></i>
                </a>
            </td>
        *@
        </tr>
    }
    </tbody>
</table>
<button asp-controller="Developer" asp-action="AddOrEdit">New</button>
</body>
</html>